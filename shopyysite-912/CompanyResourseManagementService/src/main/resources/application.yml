spring:
  application:
    name: shopyysite-912

  datasource:
    url: jdbc:postgresql://localhost:5432/shopify_db
    username: postgres
    password: Wow3password
    driver-class-name: org.postgresql.Driver

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 1088
