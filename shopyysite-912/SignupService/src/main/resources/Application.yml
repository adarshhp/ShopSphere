spring:
  application:
    name: shopyysite-912

  datasource:
    url: jdbc:postgresql://localhost:5432/CustomerCom
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 2087
